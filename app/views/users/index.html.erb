<div class="flex-1 flex flex-col overflow-hidden">
  <%= render './shared/user_dashboard' %>
  <div class="font-semibold py-4 px-6">
    <%= link_to "#{current_user.requests.count} PTO Requests", user_requests_path(current_user), class: "hover:text-red-300" %>
  </div>
  <%= link_to "New User", new_user_url %>

</div>
